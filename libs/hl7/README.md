# hl7

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build hl7` to build the library.

## Running unit tests

Run `nx test hl7` to execute the unit tests via [Jest](https://jestjs.io).
